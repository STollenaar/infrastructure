# Scripts, included by configuration.yaml as `script: !include scripts.yaml`.
# A comment-only file parses to null, which Home Assistant accepts as "no
# scripts" -- matching the empty file this replaced.
